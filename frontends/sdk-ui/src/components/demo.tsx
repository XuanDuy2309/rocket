import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export const Demo = () => {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center', height: 40, backgroundColor: 'red' }}>
            <Text>Demo</Text>
        </View>
    )
}

const styles = StyleSheet.create({})
